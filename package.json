{
  "name": "core-01-dashboard-assets",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild dashboard/frontend/editor.js --bundle --format=iife --outdir=dashboard/static --entry-names=editor.bundle --loader:.css=css --loader:.ttf=dataurl --loader:.woff=dataurl --loader:.woff2=dataurl",
    "test": "node --test dashboard/frontend/editor.test.mjs"
  },
  "dependencies": {
    "@milkdown/crepe": "7.22.1"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
